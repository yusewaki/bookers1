<% if model.errors.any? %>
  <div class="alert">
    <p><span><%= model.errors.count %> errors prohibited this book from being saved:</span></p>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
